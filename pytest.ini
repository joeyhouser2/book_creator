[pytest]
testpaths = tests
addopts = -q --strict-markers
filterwarnings =
    ignore::DeprecationWarning:ebooklib.*
# Tests that need the sibling `latin` corpus or downloaded fonts skip rather
# than fail, so the suite is still useful on a fresh clone. See tests/conftest.py.
